#include<stdio.h>
int main()
{
    // assignment operator -> used to replace a statement where an operator takes
    //                        a variable as one of its arguments then assign the
    //                        result back to the same variable... 
    //                        x=x+1; x+=1;

    int x = 10;
    // x+=2;
    // x = x+2;

    // x-=2;
    // x = x-2;

    // x*=2;
    // x = x*2;

    // x/=2;
    // x = x/2;

    // x%=2;
    // x = x%2;

    printf("%d\n",x);

    return 0;
}