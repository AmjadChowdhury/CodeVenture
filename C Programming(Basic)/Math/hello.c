#include<stdio.h>
#include<math.h>

int main() 
{
    double a = sqrt(9);
    double b = pow(2,3);
    int c = round(3.44);
    int d = ceil(3.75);
    int e = floor(3.58);
    double f = fabs(-100);
    double g = log(3);
    double h = sin(45);
    double i = cos(45);
    double j = tan(45);

    printf("Ans is %lf\n",j);


    return 0;
}